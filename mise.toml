[tools]

# Core dependencies
just = "1.40.0"
"ubi:leovct/kurtosis-test" = "v0.0.9"
"ubi:kurtosis-tech/kurtosis-cli-release-artifacts[exe=kurtosis]" = "1.20.0"
"yq" = "v4.44.3"

# lint
"pipx:ethereum-optimism/kurtosis-lint" = "v0.0.7"
